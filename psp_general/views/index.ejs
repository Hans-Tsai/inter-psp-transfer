<!DOCTYPE html>
<html>
    <head>
        <title>PSP General</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-wEmeIV1mKuiNpC+IOBjI7aAzPcEZeedi5yW5f2yOq55WWLwNGmvvx4Um1vskeMj0"
            crossorigin="anonymous"
        />
        <style>
            body {
                background-color: ivory;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: flex-start;
                height: 100vh;
                margin: 0;
                padding-top: 20px;
            }
            
            .btn {
                width: 200px;
                margin-top: 20px;
            }
        </style>
    </head>
    <body>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/psp_general">首頁</a></li>
                <li class="breadcrumb-item active" aria-current="page">PSP General</li>
            </ol>
        </nav>
        <div class="container">
            <h2 class="my-4">PSP General (SCA)</h2>
            <div class="alert alert-warning d-flex align-items-center" role="alert">
                <!-- <svg class="bi flex-shrink-0 me-2" width="24" height="24" role="img" aria-label="Warning:">
                    <use xlink:href="#exclamation-triangle-fill" />
                </svg> -->
                <div>
                    請注意，若您<strong>尚未完成二次身份驗證</strong>，您將無法使用<strong>跨機構轉帳</strong>服務。<br />
                    請先使用<strong>指紋辨識</strong>進行二次身份驗證。完成後，您即可順利進行跨機構轉帳操作。
                </div>
            </div>

            <label for="psp">支付服務商: </label>
            <input type="text" id="psp" value="<%= userInfo.psp %>" disabled />
            <div class="psp err"></div>
            <label for="username">使用者名稱: </label>
            <input type="text" id="username" value="<%= userInfo.name %>" autocomplete="username webauthn" disabled />
            <div class="username err"></div>
            <label for="account">使用者帳戶: </label>
            <input type="text" id="account" value="<%= userInfo.account %>" disabled />
            <div class="account err"></div>

            <div>
                <button type="button" class="btn btn-primary" onclick="location.href='/psp_general/authenticate' + window.location.search">
                    二次身份驗證-登入
                </button>
                <button type="button" class="btn btn-success" id="verify-register" onclick="location.href='/psp_general/register' + window.location.search">
                    二次身份驗證-註冊
                </button>
            </div>
        </div>
    </body>

    <!-- 引入 Bootstrap JS -->
    <script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-p34f1UUtsS3wqzfto5wAAmdvj+osOnFyQFpp4Ua3gs/ZVWx6oOypYoCJhGGScy+8"
        crossorigin="anonymous"
    ></script>
    <script src="https://unpkg.com/@simplewebauthn/browser/dist/bundle/index.umd.min.js"></script>

    <script>
        window.onload = function () {
            let params = new URLSearchParams(window.location.search);
            let isVerified = params.get("isVerified");
            let button = document.getElementById("verify-register");
            if (Boolean(Number(isVerified))) {
                button.disabled = true;
            } else {
                button.disabled = false;
            }
        };
    </script>
</html>
